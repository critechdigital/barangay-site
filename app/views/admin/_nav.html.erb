<nav class="bg-white shadow-sm">
  <div class="max-w-6xl mx-auto px-4 py-3 flex justify-between items-center">
    <div class="flex items-center gap-6">
      <%= link_to "🏘️ BarangaySite", admin_root_path, class: "font-bold text-blue-700" %>
      <span class="text-gray-300">|</span>
      <span class="text-sm font-medium text-gray-700"><%= current_site&.name || "My Site" %></span>
    </div>
    <div class="flex items-center gap-4 text-sm">
      <%= link_to "Dashboard", admin_root_path, class: "text-gray-600 hover:text-blue-700" %>
      <%= link_to "Announcements", admin_announcements_path, class: "text-gray-600 hover:text-blue-700" %>
      <% if current_site&.published? %>
        <%= link_to "View Site →", public_site_path(current_site.slug), class: "text-green-600 font-medium", target: "_blank" %>
      <% end %>
      <%= button_to "Logout", logout_path, method: :delete, class: "text-gray-400 hover:text-gray-600" %>
    </div>
  </div>
</nav>
