<% content_for(:title) { "New Announcement — Admin" } %>
<%= render "admin/nav" %>

<div class="max-w-2xl mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold mb-6">New Announcement</h1>
  <%= render "admin/announcements/form", announcement: @announcement %>
</div>
