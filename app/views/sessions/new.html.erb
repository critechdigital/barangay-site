<% content_for(:title) { "Log In — BarangaySite" } %>

<div class="min-h-screen flex items-center justify-center bg-gray-50 py-12 px-4">
  <div class="max-w-md w-full">
    <div class="text-center mb-8">
      <h1 class="text-3xl font-bold text-blue-700">🏘️ BarangaySite</h1>
      <p class="text-gray-500 mt-2">Welcome back</p>
    </div>

    <div class="bg-white p-8 rounded-xl shadow-sm border border-gray-200">
      <h2 class="text-xl font-bold mb-6">Log in</h2>

      <%= form_with(url: login_path, class: "space-y-4") do |f| %>
        <div>
          <%= f.label :email, "Email", class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.email_field :email, placeholder: "you@email.com", class: "w-full px-4 py-3 border border-gray-300 rounded-lg" %>
        </div>
        <div>
          <%= f.label :password, "Password", class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.password_field :password, class: "w-full px-4 py-3 border border-gray-300 rounded-lg" %>
        </div>

        <%= f.submit "Log In", class: "w-full py-3 bg-blue-700 text-white font-bold rounded-lg hover:bg-blue-800 cursor-pointer" %>
      <% end %>

      <p class="text-center text-gray-400 text-sm mt-4">
        Don't have an account? <%= link_to "Sign up free", signup_path, class: "text-blue-600 hover:underline" %>
      </p>
    </div>
  </div>
</div>
